[project]
name = "fastapi-ecs-practice"
version = "0.1.0"
description = "FastAPI の最小 API を ECS Fargate + ALB にデプロイする学習用プロジェクト"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.0",
    "uvicorn[standard]>=0.50.0",
    "httpx>=0.28",
    "boto3>=1.43.40",
]

[dependency-groups]
dev = [
    "cfn-lint>=1.52.1",
]
